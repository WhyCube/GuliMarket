spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.233.10/mail_pms
    driver-class-name: com.mysql.jdbc.Driver

  cloud:
    nacos:
      discovery:
        server-addr: 192.168.233.10:8848
  application:
    name: product
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml

  #设置主键生成规则为自增
  global-config:
    db-config:
      id-type: auto
      logic-delete-value: 1
      logic-not-delete-value: 0
server:
  port: 12000
logging:
  level:
    com.why.gulimarket: debug